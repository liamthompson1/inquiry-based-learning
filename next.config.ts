import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Domains served
  // iblearning.space   → student interface
  // teacher.iblearning.space → teacher interface
  // Middleware handles the routing split (src/middleware.ts)
};

export default nextConfig;
